# 综合测试 V7：陀螺仪调度与恢复修复

本文为历史版本。V8 按用户的新要求引入自动恢复与降级，替代文中的
“异常后须重新选择模式”等旧规则，见 `COMPREHENSIVE_V8_AUTO_RECOVERY.md`。

## 来源与边界

- 分支：`test/comprehensive-v7-gyro-fix-20260908`。
- 工作树起点：主干 `5be3be07492a5cdafa7df812737ec778e92a8ac8`。
- 保留 V6 的八个顺序增量 `ae8fdac` 至 `88165e3`；本次修复建立在
  `88165e3d8d4d35451c6e10af8c011862490acf4f` 上。
- 模式 1 保留与模式 2 共用的最新寻线入口，绕障转角使用 MPU6050；
  模式 3/4 保留共享 yaw 的入弧、半圆、出口门控。模式 2/5 不依赖 IMU
  反馈，rc.4 音频控制、曲目记忆、K210 脚本与模型保留。
- 这是独立综合测试候选，不是主干或板上程序。本次不修改
  `PROJECT_STATE.md`，不连接串口、不烧录、不操作小车。

## 已修复的可复现问题

1. **主循环先读 IMU、后执行显示/诊断、再转弯时可能误报过期。**
   GyroTurn 在 Start/Task 使用角度前补读同一份 MpuYaw 服务，模式 3/4
   直接使用 yaw 前也补读。随后重新取 HAL 时钟判定新鲜度，不重置原点。
   FIFO 消费和 5 ms 轮询门限防止多个调用者重复积分。
2. **把每次 8 帧预算误当作 FIFO 容量，或未读 FIFO 就因 80 ms 延迟锁死。**
   先检查实际 FIFO 状态，再按每次最多 8 帧/96 字节分批积分。完整历史
   保留到下次；积压未清完时 `IsReady` 返回假。真实溢出仍锁定故障。
3. **上电尚未标定便运行模式 2，10 秒后无法自行标定。**
   未静止时进入 WAIT_STATIONARY，排空运动期间数据；回到 STOP 且轮速/
   输出为零后，重新收集 200 帧。运动等待不占用静止标定期限。
   静止但姿态/数据连续无效超过 10 秒仍报标定故障。
4. **暂时数据间断被永久当成需要重标定的故障。**
   30 ms 无新帧使当前转弯中止，归类为 DATA_GAP；补读成功后可恢复
   READY，静止 STOP 下只清该暂态故障。仍须新的模式命令才能运动。
   连续无完整帧超过 250 ms 才锁定 STALE。总线、溢出、量程、驱动、
   转向错误、无进展及最终角度误差故障不会被暂态清除接口抹去。
5. **旧模块导出包的测试入口依赖未导出的产品集成文件。**
   分离独立模块测试入口与整车组合测试入口，导出包可独立运行模块/
   总线回归；完整工程额外编译真实 DriveBase/绕障组合测试。

## 诊断与操作含义

后续获准读取串口时，可在 STOP 下发送 `g`。原有字段保留，新增：

| 字段 | 含义 |
|---|---|
| `PENDING` | 尚未处理的完整 FIFO 帧数；非零时不可据此做当前角度控制 |
| `FIFO_PEAK` | 本次初始化以来见到的最大 FIFO 字节数 |
| `GAP_MAX` | 初始化完成后最大的服务间隔，毫秒 |
| `BACKLOG` | 单次看到超过 8 帧的累计次数 |
| `S=4` | WAIT_STATIONARY，等待静止重新开始标定 |
| `TURN_F=7` | 短暂数据缺口导致当前动作中止，尚未满足静止/新鲜清除条件 |

本次新增量仅存在 RAM；初始化会清除服务统计。硬故障修复后仍可在静止
STOP 下用 `c` 重标定。模式 1 未准备好时拒绝启动并输出诊断提示；不会
自行恢复旧转弯。模式 3/4 数据不可用时沿用 V6 的退出标志导航策略。

## 验证结果

- 正式 ARM 编译链接通过，无编译告警；text/data/bss：`96496/64/11888`。
- BIN：96564 字节，SHA-256：
  `8DB57C13DEB52EDEBD4C2910F774A964DE9F10DE70DF8F30F642A87C068D4907`。
- HEX SHA-256：
  `3A2F6EC1F5F0904C61EA3CF70CB4ED2F1256366F73B7021F8B11DEE661B8D068`。
- 以下五套回归全部通过：

```powershell
cmd /c tests\gyro_turn\run.cmd
cmd /c tests\line_recovery\run.cmd
cmd /c tests\sign_line\run.cmd
cmd /c tests\dfplayer\run.cmd
cmd /c tests\vision_line_v4\run.cmd
powershell -NoProfile -ExecutionPolicy Bypass -File .\build_unified_motion.ps1
```

新增用例涵盖 35/81 ms 延迟、32 帧分批追赶、重复调用不重复积分、15 秒
未静止等待后重新标定、旧 FIFO 数据不能充当新静止样本、真实溢出与总线
故障、暂态故障不自动续转。既有计时回绕及角度异常测试保留。

组合测试使用真实 MpuYaw、GyroTurn、LineObstacleBypass、LineBypassTravel
和 DriveBase，仅替代传感器总线、编码器、电池及电机引脚。验证左右转弯、
制动稳定后交给前进、红外提前结束、单轮停转、轮子转而车身不转和 STOP。
它使用人为设定的轮速惯性与 yaw 响应，不能当作真实地面模型。

## 保留的实测风险

组合测试中，响应较大的模拟车身会因现有反向制动脉冲而回弹；90° 请求
最终为 ±84.741°，正确触发 ACCURACY，未误报完成。另一响应参数下为
±86.757°，进入原有 ±4°完成窗口。这里只证明故障判定及控制交接正确，
不据此调整实车制动参数。25 ms 提前量、反向制动脉冲、IMU 安装方向、
偏置稳定性和模式 3/4 角度门限都仍需获准后实车验证。

未验证硬件通信、烧录、轮子架空或地面行驶；电脑测试不能证明塑料布
赛道上的转角精度或锐角通过率。

## 交接

V6 历史说明保留在 `COMPREHENSIVE_V6_GYRO_CANDIDATE.md`；本文是 V7 的
调度与恢复规则。最终提交 SHA、远端核验、构建日志和固件哈希随交接包
提供。已有 V6 的集成方只需评审新增修复；从当前主干集成则需要整个
V7 分支的八个 V6 增量及本次修复，不能只拣选最后一个提交。

由“黑线避障”集成任务负责后续合并和经用户授权的烧录。旧 V6 和主干
均保留，不能将 V7 电脑端验证写成已经部署或实车验证通过。
