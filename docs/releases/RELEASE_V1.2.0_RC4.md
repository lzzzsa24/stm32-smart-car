# 音频遥控与曲目记忆候选版 v1.2.0-rc.4

本预发行版在 `v1.2.0-rc.3` 的寻线与连续绕障主干上，加入通用
DFPlayer 音频遥控和掉电曲目记忆。功能提交为
`7dbcb84767f2ea290568357c0461ef5b6a57a7a7`，经 GitHub PR #10 合入
主干为 `65e4ccef621235f7019f8dd502db2ce3411f9b57`。

## 本次变化

- 遥控方向区中键改为播放/暂停；同一次上电内再次播放会从暂停位置继续。
- 左/右键改为 DFPlayer 原生上一曲/下一曲，按 TF 卡物理文件顺序工作，
  不要求音频使用 `/mp3/NNNN.mp3` 文件名。
- 上/下键继续每次调节 2 级音量；上电默认音量由 10/30 提高到 20/30，
  上键仍可提高到 30/30。
- 最近一次曲目的物理文件序号会追加保存到专用 Flash 页。完全断电后，
  中键会从记忆曲目的开头播放；标准串口协议不能可靠保存毫秒播放进度。
- 当前曲目继续默认单曲循环；STOP、模式切换和车辆安全告警仍可抢占并
  停止外部音频，不改变电机控制优先级。
- UART4 RXNE 中断读取当前 TF 曲目反馈，用实际物理序号修正软件预测；
  UART TX 仍为非阻塞发送。

## TF 卡与 Flash 边界

- 插拔 TF 卡后必须让 DFPlayer 模块本身彻底断电再上电；仅复位 STM32
  不能保证模块重新识别 TF 卡。
- 文件的物理顺序通常受复制到 TF 卡的先后影响，不保证与 Windows 的
  文件名排序一致。更换或重排 TF 卡后，记忆序号可能指向另一首音频。
- 链接脚本把应用区截止到 `0x0807EFFF`，保留：
  - `0x0807F000..0x0807F7FF`：DFPlayer 曲目记忆；
  - `0x0807F800..0x0807FFFF`：既有电机/转向标定。
- 普通复位和断电不会清除这两页；整片擦除会清除。后续烧录需要同时
  避免擦除两页，才能保留曲目与标定。

## STM32 产物与验证

- 精确构建源码：主干合并提交
  `65e4ccef621235f7019f8dd502db2ce3411f9b57`。
- ARM 编译和链接通过：text/data/bss 为 `90312/64/11728`。
- BIN 大小：90380 字节。
- BIN SHA-256：
  `56A9FD8D9DFA5CF1903C7608EE1552919B66D5213AC3FABAB3AD0BCC346408D8`
- HEX SHA-256：
  `3965EE4492E87AB0C90BEDA37BB7287055FF310D6523FC3343EEF35B6F9E6D04`
- DFPlayer 协议、遥控映射、播放/暂停、通用上一曲/下一曲、曲目查询、
  音量上下限、追加记录、重启恢复、满页延迟擦除测试全部通过。
- 完整 line-recovery/load/bypass、sign-line 和 vision-line-v4 回归通过。
- 本 Release 的精确 BIN 尚未烧录，也没有新的听音、悬空或地面测试证据。
  当前实物仍是先前记录的 `4589a25` 测试固件。

## K210 资产

K210 程序和模型相对 rc.3 没有变化，仍提供两套互斥选择：

- 模式 3/4：`k210-mode34-sign-main.py`，配合
  `k210-mode34-road-sign.kmodel`；
- 模式 5：`k210-mode5-visual-line-main.py`，不使用标志模型。

同一时刻只有一个脚本能够作为 K210 的 `/sd/main.py`。本次发布没有
连接、改写或重新验证实物 K210。

## 证据边界

本次确认的是源码回归、电脑端 ARM 构建以及 GitHub 资产校验。没有执行
STM32/K210 烧录、读回、GO、悬空或地面测试；这些步骤必须与编译成功
分开记录。
