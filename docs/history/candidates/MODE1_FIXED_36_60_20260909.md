# 固定绕障两段距离按用户指定倍数调整

role: 独立参数修改，交由“黑线避障”集成。

start_commit: 2c182e3cd2c6428591122ebce76aebda59003c27

result_commit: 本说明所在提交，完整 SHA 见交接包。

横向段 240 mm × 1.5 = 360 mm；旁侧段 120 mm × 5 = 600 mm。
只修改固定路线的这两个长度，同步代码注释与原有回归断言。
45°回线、速度、红外关闭、提前超声阈值、旧朝向回波隔离均保持。
LineBypassTravel 的超时根据目标计数及速度计算，随距离自动增加。

files_changed: Core/Src/line_obstacle_bypass.c、Core/Inc/line_obstacle_bypass.h、
Core/Src/main.c、tests/gyro_turn/test_real_drive.c、test_return_gate.c、本说明。

verification_completed:

- 完整 gyro_turn：360/600-mm 请求及四轮计数进度，IR开/关的左右镜像
  整链路线、5秒连续斜行、接线、STOP、回退、计时回绕和旧回波隔离通过。
- 完整 line_recovery 两种搜索速度及模式绑定检查通过。
- 正式 ARM 构建通过：text/data/bss=102852/64/12032，BIN=102920字节。
- git diff --check、项目状态检查通过；增量对最新综合6894af1的只读
  git apply --check通过。相关固定路线源码在修改前与该综合版本相同。

BIN SHA256: 85F2F39482FCD92F360E379B9F08CE45160B6BE4A714CC042A784A61654041C7

HEX SHA256: 1282AE5292E13DC0FC2E2D647284E87F557A3361B100AE5696A3F1BC5A564B53

not_verified: 未用串口、未烧录、未移动小车，真实地面距离未验证。
未改PROJECT_STATE.md、未推送。

risks_or_assumptions: 这是用户指定的路程参数放大，并非测得的编码器标定。
轮径47mm和1040计数/圈不变；实际车体位移仍受滑移和计数准确性影响。

integration_notes: 最新综合已包含此前固定路线和测距修复，仅cherry-pick
本提交，然后回归并重编译综合固件。工作树BIN仅为构建证据，不整体替换
综合分支。撤销本提交可恢复24/12cm。
