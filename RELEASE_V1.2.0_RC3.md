# 全模式整合候选版 v1.2.0-rc.3

本预发行版在 `v1.2.0-rc.2` 基础上，将已经单独晋升的连续绕障短距离
行驶和当前寻线控制正式组合进主干。功能源码提交为
`f54363971bc3111acdda33fbed91882a71305bc7`。

## 本次变化

- 模式 1 的 20/40 mm 绕障前进与倒退不再进入共享位置控制器的末端
  点动区，改为四轮同向连续速度控制，短距离行驶上限为 1800 CPS。
- 最外侧传感器短暂单独识别黑线时，保留内侧 0、外侧 2200 CPS 的
  缓慢前进转向。
- 同一最外侧传感器连续独亮 120 ms 后，升级为镜像的
  -2200/+2200 CPS 有动力原地纠偏；出现任意其他传感器组合、超过
  30 ms 的采样间隔、队列覆盖、复位或 STOP 时立即解除。
- 丢线自动搜索保持静音；红外遥控的手动预设音频功能不变。
- 相邻双探头正向差速、横线优先、强边缘方向交接、显式零速度上限、
  四轮编码器和电机方向映射保持现有策略。

综合测试版中的 10 秒标志探测方向保持提交 `4c345b8` 仍处于测试状态，
没有进入本 Release。

## STM32 产物与验证

- ARM 编译和链接通过：text/data/bss 为 `84208/64/11592`。
- BIN 大小：84276 字节。
- BIN SHA-256：
  `36F1FB5C78483CA09B0B33A7ACE55C89713CFC6487E52598464D235FC7A384D6`
- HEX SHA-256：
  `D63E0222BE881FC775BC18A9D2C50D0FBE514F25A3E1586F6ACBD1DF50840B3F`
- 完整 line-recovery/load/bypass 套件在两档搜索速度下通过，包括
  120 组可见线状态、119/120 ms 边界、60 组镜像中断、ISR/队列/计时器
  回绕、四轮实际控制符号、连续绕障、故障和 STOP 所有权。
- 未修改的 sign-line 与 vision-line-v4 回归也全部通过。
- 此 Release 的精确主干 BIN 尚未烧入实车。当前车上测试固件
  `f34e7dd` 的寻线部分相同，但额外包含未合并的 10 秒标志保持实验，
  因而不能把其烧录结果当作本 BIN 的烧录证明。
- 尚未进行本版本的悬空或地面测试。

## K210 资产

K210 程序和模型相对 rc.2 没有变化，仍提供两套互斥选择：

- 模式 3/4：`k210-mode34-sign-main.py`，配合
  `k210-mode34-road-sign.kmodel`。
- 模式 5：`k210-mode5-visual-line-main.py`，不使用标志模型。

同一时刻只有一个脚本能够作为 K210 的 `/sd/main.py`。本次发布没有
连接、改写或重新验证实物 K210。

## 证据边界

电脑端构建和主机回归已完成，但本 Release 的精确 BIN 尚无烧录读回、
悬空或地面测试证据。编码器验证轮子运动，不直接证明车体纠偏角度；
实车测试应保持遥控 STOP 随时可用。
