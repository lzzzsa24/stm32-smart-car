# 综合测试 V9：模式 1 无障碍寻线的连续性

用户观察：模式 2 寻线较正常，模式 1 在前方和两侧都无障碍物的普通黑线
路段也一顿一顿，并偶尔短鸣。共享状态记录板上已烧录 V8 `c767baa`。
本次只修复代码中可复现的超声干扰路径，不把它宣称为所有实车顿挫的唯一原因。

## 分支与来源

- 独立分支：`test/comprehensive-v9-mode1-continuity-20260909`。
- 从当前 main `1ce3b9edca9d43d16ba6506f16a13f0ed5e58183` 创建工作树，
  在该工作树合入已烧录 V8 `c767baa158a25cbf411aae6c1dadb5b631a2e51e`。
- 合成基线 `4993ccb` 的控制源码与 V8 一致，并保留 main 的最新部署记录。
- 本次未改 canonical main、PROJECT_STATE.md、模式 2 算法、驱动参数、
  IMU 或 K210；不访问串口，不烧录、不复位、不操作小车。

## 找到的问题

1. 已在正常前进时，单次 OUT_RANGE 会调用停车回调。应用的该回调执行
   `DriveBase_Stop(DRIVE_STOP_BRAKE)`，并将寻线速度上限置零。
2. 无回波降级已清除三点滤波历史，偶尔一帧正常远距离回波就清除降级
   标志；此时滤波尚无有效中值，FORWARD 分支随后又刹车、退回 WAIT_SAFE。
3. 测距驱动不返回结果时，250 ms 过期至 700 ms 降级之间存在停车区间。
4. 蜂鸣器把无回波降级定义为每秒 80 ms 短鸣，普通减速区也每 600 ms
   短鸣；不需要真正确认障碍就能响。

新增 `test_continuity` 在未修复 V8 源码上失败：正常前进后注入一次
OUT_RANGE，停车回调计数变为非零。旧版本复现日志在交接包
`logs/before-fix.log`，这是预期失败证据。

## 修改后的行为

- 在已前进且启用降级的综合模式中，超时、越界或数据过期只发布连续
  低速上限；不调用停车回调，不清空应用的寻线方向历史。
- 降级保持到连续三次新有效测量形成中值才解除。一帧孤立正常回波不会
  让小车在降级与等待之间切换。正常无新包的主循环不重复计数。
- 超时/越界或超过 250 ms 的有效测量间隔会清除旧的近障确认与滤波
  历史；相隔很久的两个近距离样本不能拼成连续确认。
- 5 cm 临界回波仍立即触发避障；原有动态阈值内连续两次近障仍优先
  接管。恢复期没有屏蔽真实障碍，未显式启用降级的其他调用者保持原策略。
- 无回波、普通测距降级和未确认距离不再自动短鸣。确认后的超声避障
  提示、驱动/绕障故障提示与用户手动播放保留。
- 未取得任何初始可信数据时仍保留 V8 的有限等待/降级；本次目标是消除
  正在寻线期间被坏回波反复打断，而非修改全部模式启动行为。

## 验证

正式 ARM 编译无告警，text/data/bss = `99320/64/11976`。

- BIN：99388 字节，SHA-256：
  `CB4C3B454DADD0ABE791E732B07E1D01CF993AC70A171D43D338BD15CFA783A8`。
- HEX SHA-256：
  `9DE1BED791A519FF63BAAA173CC902AF949182016DB476CC64509AD93F8B14AD`。
- gyro_turn、line_recovery、sign_line、dfplayer、vision_line_v4 五套回归通过。
- 新增回归覆盖 100 轮正常/超时/越界交替、孤立正常帧、连续三帧恢复、
  驱动无结果、近障确认被错误/时间间隔隔断、临界近障与未启用降级的
  兼容行为；已确认这些无障碍噪声序列不再发出停车回调。
- 主循环绑定检查确认 KEY1/KEY2 仍共用寻线入口，超声和绕障控制权保留，
  测距降级与距离值不再触发周期短鸣。

```powershell
cmd /c tests\gyro_turn\run.cmd
cmd /c tests\line_recovery\run.cmd
cmd /c tests\sign_line\run.cmd
cmd /c tests\dfplayer\run.cmd
cmd /c tests\vision_line_v4\run.cmd
powershell -NoProfile -ExecutionPolicy Bypass -File .\build_unified_motion.ps1
```

## 交接与限制

由“黑线避障”集成任务接手。已有 V8 的分支只需本次修复提交；从当前
main 集成需同时包含 V8 依赖，不能只拣选本修复后宣称拥有完整 V9。
合并后重新构建核验，只有明确授权才可烧录。

未做新实车测试。红外误触发、真实驱动故障也可能产生顿挫/报警，本次
没有现场日志来排除它们；已经修复的是上述能由代码回归确认的超声路径。
