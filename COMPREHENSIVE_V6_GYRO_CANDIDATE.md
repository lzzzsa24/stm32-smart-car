# 综合测试 V6：MPU6050 多模式候选

本文保留 V6 当时的来源与验证记录；后续 V7 调度、FIFO 和标定恢复修复
见 [COMPREHENSIVE_V7_GYRO_FIX.md](COMPREHENSIVE_V7_GYRO_FIX.md)。

## 版本边界

- 分支：`test/comprehensive-v6-gyro-20260908`
- 起点：当前主干 `5be3be07492a5cdafa7df812737ec778e92a8ac8`
- 回滚标签：`rollback/2026-09-08-before-comprehensive-v6-gyro`
- 功能整合提交：`554d5aa4e084206c93cd50bd63ac09e2fd308ce7`
- 状态：仅本地候选；未合并主干、未推送、未烧录、未做实物测试。

本候选没有把旧综合分支整体覆盖到主干，而是从当前主干重新组合仍然有效的
增量，因此保留 rc.4 的通用上一/下一曲、播放暂停、曲目记忆和 Flash
保留区，也不会重新引入已经被否定的固定音频文件名逻辑。

## 各模式更新审计

| 模式 | 本次采用的更新 | 最终行为 |
|---|---|---|
| STOP | `842fc1/74526f4` 的共享 MpuYaw 服务 | 静止约2秒完成200样本标定；`g`查看，`c`显式重标定 |
| KEY1 综合 | `a14aaff` + `842fc1/74526f4` | 与KEY2共用当前寻线入口；红外/超声触发绕障；直线按编码器距离，转弯按实测yaw |
| KEY2 寻线 | `a14aaff` 接口收敛，寻线算法沿用当前主干 | 不要求IMU；障碍传感器不取得电机所有权 |
| KEY3/KEY4 标志圆环 | `63f7dbc → 68db032 → ac10ba5` | 10秒是最长探测窗；传感器实时前向跟弧；实测yaw门控入弧、半圆出口和最终航向 |
| KEY5 视觉线 | 当前主干中与 `5d761e4` 一致的实现 | K210 v4整线控制不变，不要求IMU |
| DFPlayer | 当前主干 `7dbcb84` | 通用物理顺序上一/下一曲、播放暂停、曲目记忆和20/30默认音量均不变 |

旧综合V5中 `4589a25` 的固定 `/mp3/NNNN.mp3` 下一曲方式被明确排除；
其余早期寻线、连续绕障和模式5提交已经存在于当前主干，不重复拣选。
K210 的模式3/4脚本、路标模型及模式5脚本与主干逐字节一致。

## 共用 MPU6050 服务

- 接口：PB10=SCL、PB11=SDA、PE0=AD0低电平，7位地址`0x68`。
- 软件I2C使用开漏输出和现有DWT计时，不重置红外遥控使用的周期计数器；
  每次事务有12 ms硬上限，OLED继续独占PB6/PB7。
- 100 Hz FIFO，陀螺仪±500°/s、加速度±2g；按传感器10 ms采样周期积分，
  不用主循环延迟估算角度。
- 全程序只有一次启动初始化和每轮一次`MpuYaw_Task()`。模式1 GyroTurn与
  模式3/4 SignRoute读取同一份快照，不重复配置总线或分别累计yaw。
- 默认假设模块元件面朝上、Z轴朝上、俯视左转为正。实物方向必须用低速
  左右转验证；不能通过修改电机极性来掩盖yaw符号错误。

## 控制与失效策略

模式1的每次绕障转弯仍由四轮DriveBase速度闭环提供动力，但终点改为
MPU6050相对偏航角。距离目标20°以上使用请求CPS，接近终点逐步降至
1412 CPS，并按当前角速度作0.5°至4°的制动提前量。最终误差超过4°、
反向偏航、1.2秒无进展、传感器过期、驱动故障或动作超时都会锁存故障并
停车；不会运行中偷偷切回编码器角度。红外边界仍可提前结束当前转弯。

模式3/4的初始候选门限为：入弧转角60°至120°，沿圆环累计150°后才允许
识别外侧出口，225°为最大弧线角；出口选择至少转45°，最终航向需回到
进入圆环前的±15°。同时保留至少150 mm轮编码器行程和真实黑线组合条件。
IMU数据失效或转向方向错误只撤销标志导航，普通SL2循迹/丢线搜索继续；
它不会凭时间盲转，也不会把轮编码器差速误当作车身yaw。

手动STOP、模式切换和上电STOP仍有最高优先级。模式退出会经
`LineObstacleBypass_Stop()`取消正在运行的GyroTurn；故障必须在整车静止
后通过`c`重标定，旧的自动等待恢复不能清故障后自行再次转动。

## 电脑端验证

- ARM编译/链接：通过，text/data/bss=`95780/64/11872`。
- BIN：95848字节，SHA-256
  `AB082040ECC6863658C94D03E44F215478BCA468A2E2E75DFA5492D0D1F8E33B`。
- HEX：SHA-256
  `812D75C2E2A5F6EE6F4512D257C9C151DAB2B4B988ADF14BC64F348076A7AB18`。
- `tests/gyro_turn/run.cmd`：FIFO标定、正反转、制动、故障、总线超时、
  单例服务和两类模式调用绑定通过。
- `tests/sign_line/run.cmd`：左右入弧/半圆/出口、错误方向、过早出口、
  IMU失效、计时回绕和既有K210运行回归通过。
- `tests/line_recovery/run.cmd`：两档搜索速度、KEY1/KEY2共用入口、绕障、
  四轮DriveBase、故障及STOP所有权通过。
- `tests/dfplayer/run.cmd`和`tests/vision_line_v4/run.cmd`通过。

## 尚未验证

电脑端测试不能证明实物MPU6050能读到`WHO_AM_I=0x68`、安装方向正确、
静止标定成功或真实地面转角准确。入弧/出口角度窗也只是初始参数，必须在
实际圆环尺寸、轮胎打滑、电池电量和安装位置下验证。用户明确授权烧录前，
不得把此候选描述成已部署版本或已成功完成圆环。
