# 综合测试 V8：异常记录、自动恢复与降级

本版按用户最新要求改变异常处理目标：有限硬件出现可处理异常时，继续
尝试完成行驶；保护首先限制当前失败动作，不再把传感器/动作异常写成
需要遥控重新启动的永久 STOP。

## 来源与交接边界

- 分支：`test/comprehensive-v8-auto-recovery-20260908`。
- 独立工作树从当前 main `5be3be07492a5cdafa7df812737ec778e92a8ac8` 创建，
  再快进纳入完整 V7 `b6ba8e1ae07b52282a84018c3954410a215487af`。
- 保留旧 V7 和主干；不修改 `PROJECT_STATE.md`，不访问串口，不烧录、
  不复位、不操作小车。由“黑线避障”负责最终评审合并及获准后烧录。
- 原有四轮引脚/极性、驱动 PWM 上限、PI 与负载辅助参数未改动。

## 行为变化

| 情况 | V8 行为 |
|---|---|
| 模式 1 的 IMU 未标定/不可用 | 仍可正常寻线；绕障新动作使用编码器估计转角 |
| 绕障角度误差、无进展、方向或驱动异常 | 当前动作报告失败，应用记录原因；最多 800 ms 自动等待后取消旧动作，清锁存并恢复搜索 |
| 模式 3～5 驱动故障 | 同样进入有时限恢复，不再一直留在报警 `continue` 分支 |
| 恢复搜索 | 最长 1200 ms 后交还正常模式；当前中间传感器接触黑线可提前交还；只外侧/两外侧宽线不提前结束 |
| 一次陀螺仪动作失败 | 接下来的新绕障动作有 10 秒降级期，避免立即重试同一个失败角度控制；期间仍可运动 |
| IMU 总线、FIFO、过期、量程等故障 | 后台每秒重试一次；已有零偏可保留，实际收到新样本才重新 READY |
| IMU 重启丢失角度区间 | 递增 generation，取消旧相对目标与标志路线，不伪造丢失角度；正常寻线继续 |
| 从未完成过 IMU 标定 | 移动时等待静止，模式 1 继续编码器降级；需实际静止后才能完成首次零偏标定 |
| 模式 5 相机无数据/过期 | 应用立即使用四路传感器低速寻线；再次收到可用视觉控制结果时交还相机 |
| 模式 5 近处视觉障碍 | 改为朝图像较空一侧原地旋转；障碍消失后经两帧找线确认恢复 |
| 超声无结果、越界或持续超时 | 已启用降级的综合模式在 700 ms 无有效结果后低速运行，保持后续测量；有效近障回波仍优先避障 |
| 遥控 0、模式切换、上电 STOP | 取消自动恢复和所有旧动作，绝不由定时器自行启动 |

模式 2、3、4、5 的普通循迹输出原本已有“编码器异常记录而不锁死”的
策略，本版保留。绕障的编码器距离/角度动作仍会检测失效；区别是失败后
应用能够取消它并进入其他恢复动作，不能把失败动作直接当作完成。

## 状态和诊断

`f` 的 RAM 搜索记录新增 `gyro_fault`、`imu_fault`，与原有 drive/bypass
原因一起在清锁存前保存。`g` 增加 `RESTARTS`、`LAST_F`、`GENERATION`。
记录在 STOP 下读取，不在行驶中同步长串输出；断电/复位仍会清除 RAM。

原 `VISION_LINE_V4_OBSTACLE_STOP` 枚举值保留以兼容诊断数字，但当前含义
为原地避障。WAITING/LINK_STOP 表示视觉输入不可用，应用已接四路寻线
降级，不表示小车必须停车。外部消费者需按当前应用策略解释这些状态。

## 电脑端验证

- 五套完整回归：gyro_turn、line_recovery、sign_line、dfplayer、
  vision_line_v4 均通过。
- 新增组合回归使用真实 DriveBase、MPU 服务、GyroTurn、绕障和等待守护。
  验证角度误差后的恢复/编码器降级/10 秒后回到 gyro、无 IMU 启动、
  STOP 后定时器不能重启、总线重复失败的重试间隔与恢复后旧目标失效。
- 新增超声测试覆盖无结果、越界、超时、时钟回绕和有效近障抢占。
- 模式 5 回归覆盖左右避障方向、两帧重获及应用四路输入降级绑定。
- 原有左右转弯、实体驱动控制接口、搜索方向、速度、制动、音频和
  K210 协议/脚本/模型回归保留。
- 正式 ARM 编译链接通过，无编译告警；text/data/bss 为 `99392/64/11976`。
- BIN 99460 字节；SHA-256：
  `7268C24CD4B0C8534AD509E477495EAE6CAAB5C453DFD869ABC89FA028EE382B`。
- HEX SHA-256：
  `F661FB276C15B97D4186C9A2D7E47192BC4E7EBA7FA6C1E2B9CD284FD04994C7`。

```powershell
cmd /c tests\gyro_turn\run.cmd
cmd /c tests\line_recovery\run.cmd
cmd /c tests\sign_line\run.cmd
cmd /c tests\dfplayer\run.cmd
cmd /c tests\vision_line_v4\run.cmd
powershell -NoProfile -ExecutionPolicy Bypass -File .\build_unified_motion.ps1
```

## 实际限制

这是自动恢复候选，尚未烧录/实测。编码器估计转角仍受打滑影响；恢复
搜索可能移动旋转中心；没有首轮有效标定就没有可信 gyro 偏置；相机
降级后只能根据四路黑线运行，不能保证完成视觉路线语义。

保留电机输出上限与换向保护。故障重试无法产生硬件本来没有的转矩；
实体卡死时软件会继续尝试并记录，不能据此承诺轮子一定能转起来。
上一版发现的制动回弹仍可能导致当前角度动作失败，但现在应用会恢复，
不因它永久退出自动模式。

从 V7 集成只需本次新增提交；从 main 集成需整个 V8 依赖序列。最终
提交 SHA、构建日志和固件随交接包提供，不能用本版结果替换板上状态记录。
