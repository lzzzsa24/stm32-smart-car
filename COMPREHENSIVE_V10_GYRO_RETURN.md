# 综合测试 V10：陀螺仪 45° 回线直行与外侧接触交接

## 用户要求与角度定义

按用户示意图，以开始绕障时车头方向为 0°，把绕过障碍后向黑线方向的
偏航定义为正的“角 1”。右侧绕障时向左回转为正，左侧绕障时向右回转
为正。仅在已进入绕障返回阶段使用该规则，初始向外转动不触发。

陀螺仪实测角 1 **严格大于 45°** 且未检测到障碍时，取消当前转角动作，
改为连续同向前进，直到黑线接触或新障碍。它不是丢线搜索的转角上限，
也不是把编码器估计当作车身偏航角。

## 来源与边界

- 从当前 main `72b020813c3b47a1aaa2ada23c1500c0475495a5` 创建独立工作树，
  合入 V9 `230ea7425b0bd5e0ca8d78f1f493124dc88911ef`，合成基线 `75bc14c`。
- 本地分支：`test/comprehensive-v10-bypass-rejoin-20260909`。
- 共享记录仍为板上 V8 `c767baa`；V9/V10 尚无本任务烧录或实车记录。
- 保留 V9 的超声连续降级/非障碍短鸣修复及 V8 自动恢复。本次不修改
  canonical main、PROJECT_STATE.md、驱动极性/PI、K210 或普通模式 2 策略。
- 不访问串口、不烧录、不复位、不操作小车；后续交“黑线避障”集成任务。

## 实现

1. 绕障入口保存同一 MpuYaw 服务的 yaw 和 generation，返回时读取新鲜
   yaw 计算相对入口的向内角。发生 IMU 重启/数据间断后，旧参照不能
   触发此规则；未得到有效参照时沿用 V8/V9 的降级处理。
2. 旧返回转角在使用有效 gyro 参照时预留到 60°，给实时 >45° 判断留下
   制动提前量之外的空间。实际直行切换由 >45° 和无障碍条件触发，
   不是等待转完 60°，也不接受旧 ±8° 容差在不足 45° 时宣告满足新规则。
3. 无障碍条件检查两侧有效红外均越过各自阈值及滞回范围，且没有新鲜
   前向超声近障。绕障期间只服务测距驱动，不运行会写电机的旧超声
   避障回调。前向近障采用现有 10 cm 阈值；结果 250 ms 后过期。
4. 切换后四轮同向速度控制，使用 return_cps、上限 1800 CPS；当前模式 1
   配置对应 1800 CPS。取消 20 mm 小段、周期转向、距离/时间终点，持续
   前进寻找黑线。再次发现前方或任一侧障碍时退出直行并进行避让。
5. 复用现有 1 ms GPIO 队列，绕障控制也按时间顺序读取，不再只看主循环
   瞬间。只接受开始绕障之后、已允许回线阶段内、100 ms 内的证据。
6. 已离开原黑线后，单侧外部传感器或其同侧相邻组合的一次采样就能
   保存回线接触；中间接触仍需配置的确认次数。后续短暂全白保留接触，
   后续宽线/横线组合撤销它。过去、未来、重复和过期样本不能确认回线。
7. 窄外侧接触表示交接给寻线对正，不等于已居中。应用取出接触方向，
   进入至少 500 ms 的原有低速回线逻辑，并在同轮发布寻线目标；若信号
   已灭，仍向刚才接触的一侧搜索。超声恢复函数只重建测距状态和低速
   上限，不再在交接时刹车。原遥控停止与模式取消仍优先。

## 诊断字段

绕障遥测增加：`RYAW`（实测向内毫度）、`RV`（参照有效）、`RC`（连续
回线直行）、`CAP`（已捕获接触，显示顺序 8/4/2/1）。连续直行期间不再
使用原短段行程计数作为终点；旧 `RM`/`MM` 不是这段连续直行的完整里程。

## 验证

- 正式 ARM 编译无告警，text/data/bss 为 `101456/64/12032`。
- BIN：101524 字节，SHA-256：
  `C408DC96ECDCF4B6ECD156F076014C6D0B1A162A39F31B68C3B0B812EE265EAE`。
- HEX SHA-256：
  `1C8143508871CD09174D883AA71D190710F7C76443BDF9C859157C3EDBBD0671`。
- gyro_turn、line_recovery、sign_line、dfplayer、vision_line_v4 五套回归通过。
- 新边界测试覆盖左右镜像 44.999°/45°/45.001°、前方/左右障碍阻止切换
  及退出直行、IMU 不可用/重启、初始向外阶段、过期接触和遥控取消。
- 真实 DriveBase/IMU 服务/绕障组合测试从绕障入口走到返回阶段，左右
  均达到实测 >45° 后连续前进 5 秒，期间不进入分段停车或再次转向；
  只亮一个采样周期的外侧接触在随后全白时仍完成控制权交接。
- 另验证宽线撤销、左右外侧回线方向保留、超声无刹车恢复与新近障优先。

```powershell
cmd /c tests\gyro_turn\run.cmd
cmd /c tests\line_recovery\run.cmd
cmd /c tests\sign_line\run.cmd
cmd /c tests\dfplayer\run.cmd
cmd /c tests\vision_line_v4\run.cmd
powershell -NoProfile -ExecutionPolicy Bypass -File .\build_unified_motion.ps1
```

## 实际限制与交接

上述角度是相对入口车头方向；若入口车头未沿原黑线，不能把它当作精确
的线与车夹角。缺失回波不等于证明空旷，沿用本项目的降级取舍。单次
外侧信号也可能来自噪声；低速对正与后续真实黑线反馈用于继续判断。

没有实车/地面验证；仿真轮速惯性、gyro 输入和边界注入不能证明塑料布
上的转角精度。直行是四轮速度闭环同向目标，未新增航向保持 PID。

已有 V9 的分支可只拣选本次修复；板上/集成方若仍为 V8，需要先包含
V9 `230ea74`。从 main 集成需整个候选依赖。最终 SHA、补丁、固件及日志
见交接包，合并后重新构建核验；获准烧录后由集成任务更新部署记录。
