# 模式 3/4：四线循迹加路标识别

本实现以 `f9913016247d6f1624c73277704c3f32e802673e` 提供的 K210
阈值 0.2 识别包为视觉来源，并按用户要求替换集成固件的原模式 3/4。
原 8 字和正方形模块源码仍保留，但不再绑定按键或数字命令。

## 模式

| 输入 | 当前功能 |
|---|---|
| KEY1 / `1` | 增强循迹 + 红外/超声波绕障（未改） |
| KEY2 / `2` | 增强循迹，不避障（未改） |
| KEY3 / `3` | 增强四线循迹 + 左/右路标选向 |
| 遥控或串口 `4` | 独立 SL2 简化四线循迹 + 左/右路标选向 |
| 遥控或串口 `0` | 最高优先级停车 |

模式 4 的基础决策完整沿用独立分支 `feature/simple-four-line`：探头顺序
`X2 X1 X3 X4`，两次一致采样，PWM 表为 2400/2200/2600/2700，
全白时按最后方向持续原地搜线，未知方向默认向左。集成时仅将左右逻辑 PWM
换算为当前 DriveBase 的编码器闭环 CPS；没有移植该分支的独立启动文件或底层
电机极性映射。

模式 3 使用当前增强循迹控制器。模式 3/4 均不允许红外或超声波接管电机；
视觉断线、没有标志或识别不到标志时仍正常循迹，不会因视觉离线停车。

## 标志与选向

K210 使用实际 TF 卡路径
`/sd/KPU/road_sign_det/road_sign_det.kmodel`，显示五类检测框，但 UART 只发送
唯一的 left/right 候选：

```text
$D,<class>,<score>,<cx>,<cy>#
```

STM32 通过 USART2 PD6 的 RXNE 中断接收并严格校验。最近 5 帧内同类至少
3 票、最新两帧同类、分数不低于 20，才锁存左或右。相邻同类中心跳变超过
60 像素会清确认窗口。horn、one、two 不控制电机。

确认方向后，小车继续沿当前线行驶；只有四探头稳定呈现岔路/宽线至少 20 ms
才开始选支。左标志向左、右标志向右，以 2700 等效 PWM 连续原地转向；离开
岔路并稳定捕获中间探头至少 20 ms 后恢复对应模式的循迹。一个标志只执行一次，
完成后至少等待 1.5 s 且收到持续 0.8 s 的无目标帧才重新允许触发。

这一选支逻辑不等于已经完成圆形赛道几何标定。它不使用摄像头中心推算距离，
也不把编码器轮程声称为车身真实角度。必须分别完成架空方向/停车检查和地面
左、右岔路测试后，才能确认实车效果。

## 状态显示

OLED 第一行显示 `M3 ADV` 或 `M4 SIMPLE` 及电池；其余行显示四探头位图、
当前动作、最近视觉类别/分数/在线状态和 `IDLE/ARM/TURN/LOCK` 路由状态。
左方向点亮左 RGB 绿色，右方向点亮右 RGB 绿色；LED2 表示视觉帧当前在线。
